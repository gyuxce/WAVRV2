(function () {
  window.CUSTARA_RUNTIME = Object.assign({
    // Local API for the pilot. Change this to the deployed API URL later.
    apiBaseUrl: "http://127.0.0.1:4000",
  }, window.CUSTARA_RUNTIME || {});
})();
